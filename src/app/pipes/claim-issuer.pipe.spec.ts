import { ClaimIssuerPipe } from './claim-issuer.pipe';

describe('ClaimIssuerPipe', () => {
  it('create an instance', () => {
    const pipe = new ClaimIssuerPipe();
    expect(pipe).toBeTruthy();
  });
});
