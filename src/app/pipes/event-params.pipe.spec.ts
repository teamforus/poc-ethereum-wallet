import { EventParamsPipe } from './event-params.pipe';

describe('EventParamsPipe', () => {
  it('create an instance', () => {
    const pipe = new EventParamsPipe();
    expect(pipe).toBeTruthy();
  });
});
