import { ClaimTopicPipe } from './claim-topic.pipe';

describe('ClaimTopicPipe', () => {
  it('create an instance', () => {
    const pipe = new ClaimTopicPipe();
    expect(pipe).toBeTruthy();
  });
});
